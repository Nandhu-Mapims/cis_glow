DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/apdchedu_cisapp?connection_limit=10&pool_timeout=20"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="8h"
PORT=2003
CLIENT_URL="http://localhost:1003"
LEGACY_FILES_PATH="/path/to/legacy/cis/files"
LEGACY_IMG_PATH="/path/to/legacy/cis/img"
LEGACY_CIS_PATH="/path/to/legacy/cis"
# Must match the legacy app's password.php constants exactly — see server/src/services/password.js.
# Optional: only set these if the legacy app's key/IV are ever rotated (update both apps together).
# LEGACY_PASSWORD_KEY="igrapixkey1"
# LEGACY_PASSWORD_IV="1234567891011121"
