services:
  backend:
    build:
      context: ./server
    env_file:
      - ./server/.env
    environment:
      PORT: 2003
      CLIENT_URL: http://localhost:1003
    network_mode: host
    restart: unless-stopped

  frontend:
    build:
      context: ./client
    depends_on:
      - backend
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "1003:1003"
    restart: unless-stopped
